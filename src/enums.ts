export enum Job {
  WebDev,
  WebDesigner,
  PM,
}
// if you want to default export
// export default Job

export enum Type {
  Video, // 0
  BlogPost, // 1
  Quiz, // 2
}

export enum Type2 {
  Video = "VIDEO",
  BlogPost = "BLOG_POST",
  Quiz = "QUIZ",
}
